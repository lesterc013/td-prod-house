export const documents = [
  'The CAGED system maps chord shapes across the guitar neck using five basic shapes — C, A, G, E, D — so a player can find the same chord in multiple positions and move fluidly between them.',

  'Trickle breathing is a freestyle swimming technique where the swimmer exhales continuously and slowly through the nose and mouth while the face is underwater, rather than holding the breath and exhaling sharply right before turning to inhale.',

  'A discrete-event simulation models a system as a sequence of events occurring at specific points in time, rather than continuously, which makes it well suited to modelling queues, arrivals, and service stages like an immigration checkpoint.',

  'gRPC uses Protocol Buffers to define services and messages in a .proto file, then generates client and server code in multiple languages from that single definition, so the client and server stay in sync on the contract.',

  'A BTO flat in Singapore is purchased directly from the Housing and Development Board and typically has a multi-year wait between booking and key collection, since the unit is often still under construction at the time of application.',
];

export const queries = [
  'How do guitarists play the same song in different keys without retuning?', // → CAGED
  'What is the right way to breathe when your face is in the water?', // → trickle breathing
  'How would you simulate a passport control queue?', // → discrete-event simulation
  'How do you keep a client and server agreeing on a data schema?', // → gRPC/protobuf
  'What is the waiting process like for government housing in Singapore?', // → BTO
];
